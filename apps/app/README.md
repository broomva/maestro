# @maestro/app

The client SPA — one Vite + React + TypeScript build, three targets: browser, PWA,
Tauri 2 shell. Placeholder scaffold; the M0 shell (token wiring, theme switching,
glass utilities) lands in **BRO-1782**.
