# @maestro/marketing — placeholder dir only

The only Next.js in the system (marketing site on Vercel). Per D-SCOPE this is a
placeholder path with **no tickets** in the functional-core build — CalSans display
font is marketing-only, never app chrome.
