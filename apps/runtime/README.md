# @maestro/runtime

The 24/7 engine — a small supervisor (Bun + TypeScript + Hono, compiled to a single
binary via `bun build --compile`). FS-as-truth, libSQL derived index. Placeholder
scaffold; the Hono `/health` skeleton lands in **BRO-1790**.
