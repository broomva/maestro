{
  "name": "maestro",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && bun run --filter @maestro/app typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check . && bun run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2",
    "@types/bun": "^1.3.14",
    "typescript": "^5"
  }
}
