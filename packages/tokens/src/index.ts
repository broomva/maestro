// @maestro/tokens — the design tokens (source of truth).
// The 158 canonical values consumed as a package, never copied.
// Real content lands in BRO-1773 (p0-tokens-package) with a drift check
// against handoff/design_handoff_maestro/build-docs/design-system/tokens/*.
export const TOKENS_PACKAGE = "@maestro/tokens" as const;
