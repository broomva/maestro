# @maestro/protocol

Shared types — events, intents, work items — imported by both `apps/runtime` and
`apps/app`. The wire contract is the same code on both sides. Placeholder scaffold;
the OrchState machine + Intent union land in **BRO-1785**.
